name: jules_desktop_client
description: Desktop Client for Jules.ai
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cross_file: ^0.3.5+2
  desktop_drop: ^0.7.1
  dotted_border: ^3.1.0
  flutter:
    sdk: flutter
  # Provider for basic state management
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
